{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0",
    "prisma": "^4.10.0"
  },
  "dependencies": {
    "@prisma/client": "^4.10.0",
    "argon2": "^0.30.3",
    "fast-jwt": "^2.2.0",
    "joi": "^17.7.0"
  }
}
